## json_schema_rules

Defines JSON schema structures for agents that output JSON (for example, quoting agents): required fields, allowed data types, nesting, and object/array layouts. The Execution Plan refers to these rules for schema validation, while the Task references them as the contract for valid output.

### Source Locations

- `_DOCS/Graph/05-Rule-Blocks-Component.md` — Examples – JSON Schema Rules
- `_DOCS/Graph/12-Agent-Types.md` — Quoting Agent – JSON Schema Rules

