## Citation Rules

- All factual claims that depend on source documents MUST include a citation.
- Citations are added at the end of the relevant span, using HTML `<sup>` tags.
- The citation payload uses a stable identifier that can be mapped back to the source.
- The visible text should stay readable; keep markup minimal and unobtrusive.

